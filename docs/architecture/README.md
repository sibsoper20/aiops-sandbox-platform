# Architecture

This directory holds the approved logical, deployment, AI investigation, and incident simulator architectures.

The core boundary is that the AIOps orchestrator may query approved telemetry and service context but may not read the Scenario Controller's internal fault state.
