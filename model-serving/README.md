# Model serving

The local model will run in a container and expose an OpenAI-compatible API.

The inference runtime and model will be selected after the Kubernetes, telemetry, and manual investigation foundations are working. The AIOps Orchestrator must remain model-independent.
